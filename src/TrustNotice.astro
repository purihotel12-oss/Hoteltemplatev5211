---
import { ShieldCheck } from 'lucide-astro'; import { hotel } from '../data/hotel';
---
{hotel.trustNotice?.enabled&&<div class="border-y border-black/10 bg-[#ebe9e1] py-3 text-xs text-[var(--muted)]"><div class="container-hotel flex items-center gap-3"><ShieldCheck size={16}/><span>{hotel.trustNotice.message}</span></div></div>}
