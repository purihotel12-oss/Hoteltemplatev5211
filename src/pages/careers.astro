---
import { features } from '../data/features';
import BaseLayout from '../layouts/BaseLayout.astro'; import SectionHeading from '../components/SectionHeading.astro';
if (!features.careers) return Astro.redirect('/404');
---
<BaseLayout title="careers"><section class="pt-40 pb-28"><div class="container-hotel"><SectionHeading eyebrow="Optional module" title="careers" description="DEMO CONTENT — REPLACE BEFORE PRODUCTI[...]