import type { Review } from './types';
export const reviews: Review[] = [
{guestName:'DEMO GUEST',reviewText:'DEMO CONTENT — REPLACE BEFORE PRODUCTION. Never publish this placeholder as a real guest review.',rating:5,date:'DEMO DATE',source:'Direct'}
];
