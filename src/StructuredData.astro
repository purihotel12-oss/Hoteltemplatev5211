---
const items = Array.isArray(Astro.props.items) ? Astro.props.items : [Astro.props.items];
---
{items.filter(Boolean).map(item => <script type="application/ld+json" set:html={JSON.stringify(item)}></script>)}
