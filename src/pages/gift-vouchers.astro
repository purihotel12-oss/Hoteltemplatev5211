---
import { features } from '../data/features';
import BaseLayout from '../layouts/BaseLayout.astro'; import SectionHeading from '../components/SectionHeading.astro';
if (!features.giftVouchers) return Astro.redirect('/404');
---
<BaseLayout title="gift vouchers"><section class="pt-40 pb-28"><div class="container-hotel"><SectionHeading eyebrow="Optional module" title="gift vouchers" description="DEMO CONTENT — REPLACE BEF[...]